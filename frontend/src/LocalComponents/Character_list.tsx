import React from 'react';
import { useParams } from 'react-router-dom';

const CharacterList = () => {
   let { id } = useParams();
   // TODO: Заполнить
   return (
      <div>
         
      </div>
   );
}

export default CharacterList;
